// 进阶教程数据 —— LNU-SecLLM 安全学习平台
// 自包含模块：纯 JS 全局赋值，由 academy.js 读取 window.ACADEMY_TUTORIALS
// 正文为标准 Markdown 字符串。注意：模板字符串内的反引号需用 \` 转义，${ 需用 \${ 转义。

window.ACADEMY_TUTORIALS = {
    categories: [
        {
            id: 'basic',
            name: '基础篇',
            articles: [
                {
                    id: 't-what-is-sectest',
                    title: '安全测试是什么',
                    difficulty: '入门',
                    minutes: 8,
                    md: `# 安全测试是什么

安全测试(Security Testing),又称渗透测试(Penetration Testing)的广义形式,是指**在获得明确授权的前提下**,模拟攻击者的思路与手段,对目标系统进行检测,以发现潜在安全弱点并验证其可被利用程度的过程。它的目标不是"搞破坏",而是"先于真正的攻击者找到问题",从而帮助组织修复。

## 与"黑客攻击"的本质区别

| 维度 | 安全测试 | 恶意攻击 |
| --- | --- | --- |
| 授权 | 有书面授权,范围明确 | 无授权,非法 |
| 目的 | 发现并报告问题、协助修复 | 窃取数据、破坏、牟利 |
| 影响 | 受控,尽量不影响业务 | 不计后果 |
| 结果 | 出具报告、闭环整改 | 隐藏踪迹 |

> 合规红线:任何测试必须在授权范围内进行。未经授权对他人系统的扫描、探测、攻击均可能触犯《网络安全法》《刑法》第 285/286 条。课程实验请只在本平台提供的靶场或自有环境中操作。

## 一次测试的典型流程

1. **授权与范围界定**:确定可测的 IP/域名、时间窗口、禁止动作。
2. **信息收集(侦察)**:摸清目标有哪些资产、开放了哪些服务。
3. **漏洞发现**:用工具与人工分析定位弱点。
4. **漏洞验证(利用)**:在授权下证明漏洞真实可利用。
5. **报告与修复**:给出风险等级、复现步骤与修复建议。

## 常见测试类型

- **黑盒测试**:几乎不掌握内部信息,模拟外部攻击者。
- **白盒测试**:掌握源码、架构,检测更彻底。
- **灰盒测试**:介于两者之间,效率与覆盖兼顾。

## 学了能做什么

掌握安全测试基础后,你能读懂扫描报告、理解漏洞成因、参与企业安全建设(SDL、应急响应),也能在本平台用 AI 助手自动化完成大部分侦察与初筛工作。后续章节会带你由浅入深地实践。
`
                },
                {
                    id: 't-ports-services',
                    title: '认识端口与网络服务',
                    difficulty: '入门',
                    minutes: 9,
                    md: `# 认识端口与网络服务

在网络里,一台主机就像一栋大楼,**IP 地址是大楼的门牌号,端口(Port)则是大楼上一扇扇的门**。每扇门后面运行着一个服务(Service)。攻击与防御的起点,往往就是搞清楚"这栋楼开了哪些门、门后是谁"。

## 端口是什么

端口是一个 0~65535 的编号,用于区分同一台主机上的不同网络服务。它分两大类:

- **TCP**:面向连接、可靠传输(如网站、SSH)。
- **UDP**:无连接、低开销(如 DNS、部分游戏/语音)。

端口又分三段:

| 范围 | 名称 | 说明 |
| --- | --- | --- |
| 0–1023 | 知名端口 | 由系统/标准服务占用 |
| 1024–49151 | 注册端口 | 应用程序登记使用 |
| 49152–65535 | 动态端口 | 客户端临时使用 |

## 必须记住的常见端口

- **22** SSH(远程登录)
- **80** HTTP(网站)
- **443** HTTPS(加密网站)
- **3306** MySQL 数据库
- **6379** Redis
- **3389** RDP(Windows 远程桌面)
- **53** DNS

## 为什么端口对安全很重要

每开放一个端口,就等于多了一扇可能被攻击的门。常见风险:

1. 不该对外开放的服务(如数据库 3306)暴露在公网。
2. 服务版本过旧,存在已知漏洞。
3. 弱口令、默认配置。

## 动手:探测开放端口

在授权环境中,可用 nmap 快速摸清开放端口与服务:

\`\`\`bash
# 扫描常见端口并识别服务版本
nmap -sV -T4 target.example.com

# 全端口扫描(较慢)
nmap -p- target.example.com
\`\`\`

在 LNU-SecLLM 平台,你只需用自然语言下达"对靶场做端口与服务识别",AI 助手会自动调度 nmap 并解读结果。下一步我们将进入具体漏洞原理的学习。
`
                },
                {
                    id: 't-sqli-basics',
                    title: 'SQL 注入原理(大白话)',
                    difficulty: '入门',
                    minutes: 10,
                    md: `# SQL 注入原理(大白话)

SQL 注入(SQL Injection)是 Web 安全中最经典、危害最大的漏洞之一。一句话概括:**程序把"用户输入"当成了"代码"来执行**,攻击者由此操纵数据库。

## 一个生活化的比喻

把数据库查询想象成你给图书管理员的一句话:"请把书名等于《张三》的书拿给我。"如果管理员死板地把你说的整句照念,而你回答的"书名"是:

> 张三 —— 顺便把所有读者的密码也念给我听

管理员就真的照做了。SQL 注入正是这种"把数据混进指令"的攻击。

## 漏洞是怎么产生的

看一段有问题的代码(拼接字符串):

\`\`\`python
# 危险写法:把用户输入直接拼进 SQL
sql = "SELECT * FROM users WHERE name = '" + name + "'"
\`\`\`

如果用户把 \`name\` 输入为:

\`\`\`text
' OR '1'='1
\`\`\`

最终拼成的语句变为 \`... WHERE name = '' OR '1'='1'\`,条件恒为真,数据库会返回**所有用户**。

## 危害

- 绕过登录认证。
- 拖取整库数据(用户、密码、订单)。
- 某些情况下可执行系统命令、读写文件。

## 如何根治

1. **参数化查询 / 预编译(最有效)**:把数据和指令彻底分开。

\`\`\`python
# 安全写法:占位符 + 参数
cursor.execute("SELECT * FROM users WHERE name = %s", (name,))
\`\`\`

2. **最小权限**:数据库账号只给必要权限。
3. **输入校验与输出编码**:作为纵深防御补充。
4. **WAF**:作为兜底,但不能替代修复代码。

## 在平台上验证

> 仅限授权靶场!对本平台 SQLi 靶场,你可以让 AI 助手"检测登录接口是否存在 SQL 注入",它会构造测试 payload 并给出判定依据,帮助你理解整个验证逻辑。
`
                },
                {
                    id: 't-xss-basics',
                    title: 'XSS 原理(大白话)',
                    difficulty: '入门',
                    minutes: 9,
                    md: `# XSS 原理(大白话)

XSS(Cross-Site Scripting,跨站脚本)与 SQL 注入是"亲戚":都是因为程序把**用户输入当成了可执行内容**。区别在于,SQL 注入欺骗的是数据库,而 XSS 欺骗的是**浏览器**——让受害者的浏览器执行了攻击者注入的 JavaScript。

## 一句话理解

你在某论坛发了一条"评论",内容其实是一段脚本。任何打开这条评论的人,浏览器都会偷偷执行这段脚本——可能被偷走 Cookie、被冒名操作。

## 三种类型

- **反射型(Reflected)**:恶意脚本在 URL 参数里,服务器原样回显,需诱导点击链接。
- **存储型(Stored)**:脚本被存进数据库(如评论区),危害最大,谁看谁中招。
- **DOM 型(DOM-based)**:漏洞在前端 JS 处理过程中,不经过服务器。

## 一个示例

页面把用户昵称直接输出到 HTML:

\`\`\`html
<div>欢迎,__NICK__</div>
\`\`\`

若昵称被设置为下面这段,浏览器就会弹窗(真实攻击中会替换成窃取 Cookie 的代码):

\`\`\`html
<script>alert(document.cookie)</script>
\`\`\`

## 危害

- 窃取会话 Cookie,冒充用户登录。
- 钓鱼、篡改页面、键盘记录。
- 蠕虫式传播(存储型)。

## 防御要点

1. **输出编码(核心)**:把 \`<\` \`>\` \`&\` \`"\` 等转义成 HTML 实体。
2. **使用安全框架**:如 React/Vue 默认转义。
3. **CSP(内容安全策略)**:限制脚本来源。
4. **HttpOnly Cookie**:让脚本读不到关键 Cookie。

> 在本平台的 XSS 靶场中练习时,请勿将测试 payload 用于任何真实站点。理解"输入即代码"的危险,是写出安全代码的第一步。
`
                }
            ]
        },
        {
            id: 'practice',
            name: '平台实战篇',
            articles: [
                {
                    id: 't-full-test-walkthrough',
                    title: '用 LNU-SecLLM 做一次完整测试',
                    difficulty: '中级',
                    minutes: 11,
                    md: `# 用 LNU-SecLLM 做一次完整测试

LNU-SecLLM 是一款 **AI 驱动的安全测试平台**:你用自然语言描述需求,平台背后的 AI Agent 会自动规划步骤、调用 nmap / nuclei 等工具、解读结果并生成报告。本文带你走一遍端到端流程。

## 前置:授权确认

> 在任何测试前,务必确认目标在授权范围内(本平台靶场或你拥有/获授权的资产)。这是不可逾越的红线。

## 步骤一:明确目标与范围

进入工作台,新建一个测试任务,填写:

- 目标(靶场地址 / IP)。
- 测试目标:如"找出可被利用的 Web 漏洞"。
- 约束:如"不要做拒绝服务测试、限速扫描"。

## 步骤二:信息收集

下达指令,例如:

\`\`\`text
请对目标做端口与服务识别,并枚举 Web 目录,告诉我有哪些值得关注的入口。
\`\`\`

AI 会调用 nmap 做服务识别,并汇总开放端口、服务版本、可疑入口点。

## 步骤三:漏洞扫描与发现

\`\`\`text
对发现的 Web 服务用 nuclei 跑一遍常见漏洞模板,按严重程度给我排序。
\`\`\`

平台会把 nuclei 的原始输出解析成结构化结果,并标注 CVSS 等级。

## 步骤四:验证与攻击链

对高危项,AI 会尝试在授权下做最小化验证,并把"信息收集 → 弱点 → 利用路径"串成**攻击链图**,帮助你直观理解风险是如何累积的。

## 步骤五:生成报告

\`\`\`text
请汇总本次测试,生成包含风险等级、复现步骤和修复建议的报告。
\`\`\`

## 小结

平台的价值在于把繁琐的工具操作与结果解读自动化,让你把精力放在**判断与决策**上。后续文章会教你如何把指令下得更高质量,以及如何读懂工具输出。
`
                },
                {
                    id: 't-prompt-quality',
                    title: '如何下达高质量的测试指令',
                    difficulty: '中级',
                    minutes: 9,
                    md: `# 如何下达高质量的测试指令

LNU-SecLLM 用 AI Agent 理解你的自然语言并执行。指令质量直接决定结果质量——**模糊的指令得到模糊的结果**。本文给出一套实用的指令编写方法。

## 好指令的四要素

1. **目标(What)**:你想达成什么。
2. **对象(Where)**:针对哪个资产/接口。
3. **约束(Limits)**:不能做什么、限速、时间窗口。
4. **期望产出(Output)**:你要的结果形式。

## 对比示例

反面教材:

\`\`\`text
帮我测一下这个网站。
\`\`\`

更好的版本:

\`\`\`text
针对靶场 http://target/ 的登录接口,检测是否存在 SQL 注入与弱口令;
请限速、不要做 DoS;最后用表格列出发现项、证据和 CVSS 等级。
\`\`\`

## 进阶技巧

- **分步下达**:先侦察,看结果,再决定下一步,而非一句话要求"全自动黑掉"。
- **提供上下文**:告诉 AI 已知信息(如"这是个 PHP 站,用 MySQL"),减少试错。
- **明确判定标准**:"只有能稳定复现才算确认漏洞"。
- **要求引用证据**:让 AI 给出请求/响应片段或工具原文,便于你复核。

## 安全护栏

平台会拒绝越权或破坏性请求。即便如此,你也应当主动避免:

- 让 AI 攻击授权范围外的目标。
- 执行不可逆的破坏性操作。

> 把 AI 当成一位"听话但需要清晰指令的实习生":你说得越具体、越结构化,它干得越好。下一篇我们来读懂它返回的扫描结果。
`
                },
                {
                    id: 't-read-scan-output',
                    title: '读懂扫描结果:Nmap/Nuclei 输出',
                    difficulty: '中级',
                    minutes: 11,
                    md: `# 读懂扫描结果:Nmap / Nuclei 输出

AI 会替你跑工具,但**看懂原始输出**能让你做出更准的判断,也能识别误报。本文讲解两款核心工具的输出。

## Nmap:端口与服务

典型输出片段:

\`\`\`text
PORT     STATE    SERVICE  VERSION
22/tcp   open     ssh      OpenSSH 7.4 (protocol 2.0)
80/tcp   open     http     Apache httpd 2.4.6
3306/tcp open     mysql    MySQL 5.7.30
\`\`\`

如何阅读:

- **PORT**:端口号/协议。
- **STATE**:\`open\` 开放、\`closed\` 关闭、\`filtered\` 被防火墙过滤。
- **SERVICE / VERSION**:服务及版本——**版本号是查已知漏洞的关键**。

关注点:数据库 3306 对外开放、服务版本老旧,都是高优先级线索。

## Nuclei:基于模板的漏洞检测

典型输出片段:

\`\`\`text
[CVE-2021-41773] [http] [high] http://target/cgi-bin/.%2e/...
[tech-detect:apache] [http] [info] http://target/
[ssl-issuer] [ssl] [info] target:443
\`\`\`

字段含义:

| 部分 | 含义 |
| --- | --- |
| 方括号模板 ID | 命中的检测项,如 CVE 编号 |
| 协议 | http / ssl / dns 等 |
| 严重程度 | info / low / medium / high / critical |
| URL | 命中位置 |

## 区分"信号"与"噪声"

- \`info\` 级别多为指纹/信息,不一定是漏洞。
- \`high\`/\`critical\` 优先核实;但需警惕**误报**:模板匹配不等于真实可利用。
- 同一问题可能被多个模板重复命中。

## 让 AI 帮你二次研判

\`\`\`text
把以上 nuclei 结果按可利用性重新排序,剔除疑似误报,并说明判断依据。
\`\`\`

理解原始输出 + 借助 AI 研判,才能既快又准。下一篇我们看平台如何把这些点连成攻击链。
`
                },
                {
                    id: 't-attack-chain-graph',
                    title: '看懂攻击链图',
                    difficulty: '中级',
                    minutes: 8,
                    md: `# 看懂攻击链图

单个漏洞往往危害有限,真正致命的是**多个弱点被串联**起来。LNU-SecLLM 会把测试过程可视化为"攻击链图",帮助你理解风险是如何一步步放大的。

## 攻击链是什么

攻击链(Attack Chain / Kill Chain)描述攻击者从初始接触到最终目标的完整路径。一个常见模型分为:

1. **侦察**:收集目标信息。
2. **初始入口**:利用某个漏洞获得立足点。
3. **权限提升**:从普通权限升到管理员。
4. **横向移动**:从一台主机扩展到内网其他主机。
5. **达成目标**:数据窃取、持久化等。

## 图里有什么

平台的攻击链图通常包含:

- **节点**:资产(主机、服务、账户)或状态(已获取某权限)。
- **边**:从一个节点到另一个的"动作",标注所用漏洞/技术。
- **颜色/权重**:表示风险等级或可利用难度。

## 一个示例路径

\`\`\`text
[外网] --弱口令--> [Web 服务器] --SQL注入读配置--> [数据库凭据]
        --凭据复用--> [内网管理后台] --提权--> [域控]
\`\`\`

## 为什么它有用

- **看清最短攻击路径**:优先切断关键边,性价比最高。
- **解释"低危叠加成高危"**:单独看每个点都不严重,串起来却很致命。
- **指导修复顺序**:修掉链条上的"咽喉"节点,往往能一举阻断多条路径。

## 实操建议

拿到攻击链图后,问自己三个问题:入口在哪?最危险的一跳是什么?切断哪条边成本最低收益最大?把答案写进报告的修复优先级里。下一篇进入进阶篇,系统梳理信息收集方法论。
`
                }
            ]
        },
        {
            id: 'advanced',
            name: '进阶篇',
            articles: [
                {
                    id: 't-recon-methodology',
                    title: '信息收集方法论:从域名到资产',
                    difficulty: '进阶',
                    minutes: 12,
                    md: `# 信息收集方法论:从域名到资产

信息收集(Reconnaissance)是渗透测试中**投入产出比最高**的阶段。攻击面摸得越全,后续越有的放矢。本文给出一套从一个域名扩展到完整资产清单的方法论。

## 总体思路

信息收集分为两类:

- **被动收集**:不直接接触目标,通过第三方数据(DNS、证书透明度、搜索引擎)获取信息,隐蔽性高。
- **主动收集**:直接与目标交互(扫描、爬取),信息更准但有日志痕迹。

> 合规提醒:即便是被动收集,后续的主动探测也必须在授权范围内进行。

## 第一步:子域名枚举

从主域名出发,找出所有子域名,扩大攻击面:

\`\`\`bash
# 被动:基于证书透明度/公开数据
subfinder -d example.com

# 主动:字典爆破解析
puredns bruteforce wordlist.txt example.com
\`\`\`

## 第二步:资产存活与服务探测

把子域名解析为 IP,确认存活并识别服务:

\`\`\`bash
# 探测存活 Web 服务并抓取标题、状态码、技术栈
httpx -l subdomains.txt -title -status-code -tech-detect
\`\`\`

## 第三步:指纹与技术栈识别

识别 CMS、框架、中间件版本,为漏洞匹配做准备。版本信息直接对应已知 CVE。

## 第四步:目录与接口发现

\`\`\`bash
# 目录爆破,寻找隐藏入口
ffuf -u http://target/FUZZ -w dirlist.txt
\`\`\`

## 第五步:汇总成资产清单

整理成结构化清单:域名、IP、开放端口、服务、技术栈、可疑入口。这张表是后续所有工作的地图。

## 在平台上一键完成

\`\`\`text
对 example.com 做完整信息收集:子域名枚举、存活探测、指纹识别,
最后给我一张资产清单表。
\`\`\`

AI Agent 会编排上述工具链并输出清单。信息收集的深度,决定了渗透测试的上限。
`
                },
                {
                    id: 't-cvss-prioritization',
                    title: '漏洞分级与修复优先级(CVSS)',
                    difficulty: '进阶',
                    minutes: 11,
                    md: `# 漏洞分级与修复优先级(CVSS)

发现一堆漏洞后,真正的难题是:**先修哪个?** CVSS(Common Vulnerability Scoring System,通用漏洞评分系统)提供了一套标准化的量化方法,帮助团队按风险排序。

## CVSS 评分概览

CVSS 当前主流为 v3.1,分数范围 0.0~10.0,对应等级:

| 分数 | 等级 |
| --- | --- |
| 0.0 | None |
| 0.1–3.9 | Low(低) |
| 4.0–6.9 | Medium(中) |
| 7.0–8.9 | High(高) |
| 9.0–10.0 | Critical(严重) |

## 基础指标(Base Metrics)

评分主要由两组因素决定:

- **可利用性**:攻击向量(网络/相邻/本地/物理)、攻击复杂度、所需权限、是否需要用户交互。
- **影响**:对机密性(C)、完整性(I)、可用性(A)的破坏程度。

一个"可远程、无需认证、无需交互、能完全控制系统"的漏洞,自然趋近 9~10 分。

## 别只看分数:结合环境

CVSS Base 分是"通用值",真正排优先级还要叠加:

1. **环境指标**:该资产对你而言有多重要?是核心数据库还是测试机?
2. **威胁情报**:是否已有公开 EXP、是否正被在野利用(KEV 清单)?
3. **暴露面**:是否对公网开放?

> 经验法则:一个 7.5 分但已被在野利用、且对外暴露的漏洞,往往应当排在一个 9.0 分但需复杂前置条件的内网漏洞之前。

## 修复优先级决策表

| 条件 | 处置 |
| --- | --- |
| Critical + 公网 + 有 EXP | 立即修复/应急 |
| High + 核心资产 | 限期修复 |
| Medium | 排期修复 |
| Low / 无暴露 | 记录、择机处理 |

## 在平台上应用

LNU-SecLLM 会自动为发现项标注 CVSS 等级。你可以进一步要求:

\`\`\`text
结合资产重要性和是否对公网暴露,给这些漏洞重新排一个修复优先级。
\`\`\`

科学的分级让有限的修复资源用在刀刃上。
`
                },
                {
                    id: 't-mcp-ai-agent',
                    title: 'MCP 与 AI Agent 的原理',
                    difficulty: '进阶',
                    minutes: 12,
                    md: `# MCP 与 AI Agent 的原理

LNU-SecLLM 之所以能"听懂人话、自己动手",背后靠的是 **AI Agent** 架构与 **MCP(Model Context Protocol,模型上下文协议)**。本文揭开它的工作原理。

## AI Agent 是什么

普通的对话模型只会"说",而 Agent 会"做"。它在大语言模型(LLM)外围加了一个循环:

1. **感知**:接收用户目标与当前环境信息。
2. **规划**:把大目标拆成可执行步骤。
3. **行动**:调用外部工具(如 nmap)。
4. **观察**:读取工具返回结果。
5. **反思迭代**:根据结果决定下一步,直到完成目标。

这就是常说的"ReAct(推理 + 行动)"范式。

## 工具调用(Tool Use)

LLM 本身不能联网或执行命令,它通过**工具调用**与外界交互:模型输出一个"我想调用工具 X、参数 Y"的结构化请求,由运行环境真正执行,再把结果回传给模型。

\`\`\`text
模型: 调用 nmap(target="靶场", flags="-sV")
环境: [执行 nmap,返回端口列表]
模型: 根据端口列表,继续规划下一步
\`\`\`

## MCP 是什么

MCP 是一种**标准化协议**,用于让 AI 应用以统一方式连接外部的"工具"与"数据源"。可以把它理解为 AI 世界的"USB 接口":

- **MCP Server**:封装某种能力(扫描器、文件系统、数据库)并对外暴露。
- **MCP Client / Host**:AI 应用,按协议发现并调用这些能力。

好处是解耦:工具开发者只需实现一次 MCP Server,任何兼容 MCP 的 AI 应用都能直接使用,无需为每个模型单独适配。

## 在本平台中的角色

在 LNU-SecLLM 中:

- 各类安全工具被包装成 MCP Server(或等价的工具接口)。
- AI Agent 作为 Host,理解你的指令、规划测试流程、按需调用这些工具,并将原始输出解析成人类可读的结论与报告。

## 安全与可控

- **权限边界**:Agent 的可用工具与目标范围受平台严格限制,拒绝越权操作。
- **可解释**:每一步工具调用与依据都可追溯,便于复核。

理解了 Agent 与 MCP,你就明白了"自然语言驱动安全测试"背后的工程逻辑——这正是 LNU-SecLLM 的核心。
`
                }
            ]
        }
    ]
};
